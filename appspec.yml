verion: 0.0
os: linux

files:
  - source: docker
    destination: /usr/src/app
hooks:
  BeforeInstall:
    - location: scripts/install_docker.sh
      runas: root
  # Install:
  ApplicationStart:
    - location: scripts/start-docker.sh
      runas: root